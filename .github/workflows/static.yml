name: Deploy static content to main

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write  # Potrzebne do commitowania i pushowania na `main`

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repozytorium
        uses: actions/checkout@v4

      - name: Create .nojekyll file
        run: echo > .nojekyll

      - name: Uruchom skrypt
        run: |
          python 1download.py

      - name: Sprawdź, czy są zmiany do commitowania
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "Zaktualizowano wyniki analizy Minerviniego"
            git push origin main
          else
            echo "Brak zmian do commitowania"
